# Product Scripts

이 디렉터리는 설치된 Podo가 사용하는 local entrypoint를 위한 자리다.

`capture_event`는 Phase 3에서 transcript adapter와 함께 구현한다. 그 전에는 `product/.codex/hooks.json`을 실제 User Workspace에 설치 가능한 기능으로 공개하지 않는다.
